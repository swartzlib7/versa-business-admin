# Agent files in this repo

Start at **`AGENTS.md`** (repo root). Human ops stay under `docs/ops/` (Ops Manual + `WORKING_WITH_VBA.md`).

Install and operate live on the Versa AGi host (`business_admin`, `business_admin_operate`). Do not invent a second skill home here for those procedures.
