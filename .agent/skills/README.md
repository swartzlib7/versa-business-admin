# Skills

This repo does not ship product skills. On a Versa AGi host, load:

- `business_admin` — COA install / orient
- `business_admin_operate` — API / operate / local enhance

Then this repo’s `AGENTS.md`, `docs/ops/BUSINESS_ADMIN_OPS_MANUAL.md`, and `docs/ops/WORKING_WITH_VBA.md`.
